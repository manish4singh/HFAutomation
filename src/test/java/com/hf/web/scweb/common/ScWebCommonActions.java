package com.hf.web.scweb.common;

import com.controllers.core.WebController;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class ScWebCommonActions extends WebController {

    private static final Logger logger = LogManager.getLogger(ScWebCommonActions.class);

    /**
     * @author Nethaji
     * @Modifier : Nethaji
     * This is the skeleton of a test method
     */
    public void method(String url, String... emailId) {
        logger.info("Your steps\n");
    }
}